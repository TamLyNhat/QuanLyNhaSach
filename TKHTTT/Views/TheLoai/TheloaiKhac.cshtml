@using TKHTTT.Models
@model List<TheLoai>
@{
    ViewBag.Title = "TheloaiKhacPartial";
    Layout = "~/Views/Layout/HomeLayout.cshtml";
}


<div class="container">
    <div class="row justify-content-center">

        <h1 class="text-success">Thể loại khác</h1>        
  
    </div>
    <div class="row justify-content-center">
        <div class="btn-group " role="group" aria-label="Basic example">
            @foreach (var item in Model)
            {
                @*<input type="button" value="@item.TenTL" class="btn btn-outline-secondary" id="@item.TenTL"/>*@
                @Html.ActionLink(item.TenTL, "LocTheoTheLoai", new { id = item.MaTL }, new { @class = "btn btn-outline-secondary" })
            }
        </div>
    </div>

</div>
